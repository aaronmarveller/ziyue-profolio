# Public video assets

`ai-english-learning-demo.mp4` is a **placeholder** for the iPhone
screen-recording demo of the AI English-learning app, served by
`/video/ai-english-learning`. It's currently a tiny 2-second silent test clip
so the page is verifiable end-to-end. The real recording isn't ready yet —
replace this file (keep the same name/path) and the page, the redirects, and
the tests keep working with no code change.
